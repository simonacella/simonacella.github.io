---
layout: null
sitemap: false
---
{%- include locale.html -%}
[
  {% for post in site.posts %}
    {%- if post.lang == site.active_lang -%}
      {%- assign search_url = lang_prefix | append: post.url -%}
    {%- else -%}
      {%- assign search_url = post.url -%}
    {%- endif -%}
    {
      "title"       : {{ post.title | jsonify }},
      "tags"        : {{ post.tags | join: ', ' | jsonify }},
      "url"         : {{ search_url | jsonify }},
      "date"        : {{ post.date | jsonify }},
      "description" : {{ post.description | jsonify }},
      "content"     : {{ post.content | searchable_text | jsonify }}
    } {% unless forloop.last %},{% endunless %}
  {% endfor %}
]
